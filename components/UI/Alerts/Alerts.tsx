'use client'

/**
 * Alerts component.
 *
 * Note: Open-Meteo does not provide weather alerts.
 * This component is kept for potential future integration with another alerts API.
 */
export default function Alerts() {
  // Open-Meteo doesn't provide weather alerts
  // This component will always return null until an alerts API is integrated
  return null
}
